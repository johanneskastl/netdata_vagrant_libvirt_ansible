---
- name: 'Gather facts for all hosts'
  hosts: 'all'
  gather_facts: 'yes'
  become: 'true'

- name: 'Netdata node configuration'
  hosts: 'netdata_nodes'
  gather_facts: 'yes'
  become: 'true'

  roles:
    - role: 'netdata-agent'
